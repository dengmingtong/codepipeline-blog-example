version: 0.2

phases:
    build:
        commands:
          - echo Build started on `date`
          - |
            echo "------------------build app image-----------------------------------------------"
            echo $CODEBUILD_SRC_DIR
            set -e
            cd $CODEBUILD_SRC_DIR
            ls
            docker build .
